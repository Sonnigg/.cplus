# INTRODUCING C+

<img src="cx/cplus-icon.png" alt="C+ Logo" width="128" height="128"> <img src="cx/cplus-header-icon.png" alt="C+ Header Logo" width="128" height="128">

Shortcut to the license : [LICENSE](./LICENSE)

Shortcut to libc+       : [libc+](./libc+/)

Shortcut to the source  : [source](./source/)

## IMPORTANT

Currently, C+ provides pre-built compiler packages for:
- [Windows x86 and x86_64](./winzips/)
- [Linux x86 and x86_64](./tar-bz/)
- [Linux ARM64 and aarch64](./arm-arch/)

Windows ARM64 is currently not supported.

## What is C+?
Simply put, **C+ started as my C++ dialect, but grew into its own language**. It gives a lot of functionality beyond C's simple structs, as it introduces proper namespaces, scoped enums, and better structs that can actually have methods with the lowering being C-like (more on that below).

C+ introduces the concept of deferring actions to the low-level languages. This design was heavily inspired by Go's `defer` and also took exactly that form. The new C+ defer (old C+ defer was strictly lexical-bound) will always emit the code inputted to it before every return AND at the end of the lexical scope, meaning you do return before the scope ends (which is normal because return comes before the closing brace), the generated C code will still have the defer code AFTER the return. This does not lead to double-frees, because it would run after a return, therefore is unreachable.
> Why did I choose 'defer'? Because I didn't know what else to call it.
> Another note: defer changed its design to be Go-like from the original weird lexical scope design.

### About defer

The compiler duplicates the deferred code before every `return`, and also emits it at the normal end of the scope. If execution returns early, the end-of-scope copy is unreachable, so the deferred code still executes exactly once.

## Why choose C+?
Something you might say: "There is no reason as to why you shouldn't just use C++", and I agree with that. But let's be honest, we've all been there when we wanted to use C but just didn't because of all its historical quirks such as no namespaces, and so much more, that's why I made C+, to not carry around C++ baggage and to fix C's old mistakes! You might agree or disagree, but in the end, it's your decision afterall.
> Why am I not sounding more 'USE C+!!!'? Because I'm being honest and value your opinion and choices.

## Code examples in C+

General struct showcase:
```cx
namespace app
{
    struct Player
    {
        int health;

        void damage(Player *self, int amount) // no need to name the first parameter self, can also be this or whatever! But it has to be there
        {
            self->health -= amount;
        }
    }
}

int main()
{
    app::Player player = app::Player {
        health: 100,
    };
    player.damage(10);
    return 0;
}
```

"Hello, World!" showcase:
```cx
#include <libc+>

int main()
{
    std::hello();
    return 0;
}
```

More code examples can be found in the [Example folder](./examples/), such as [hello.cp](./examples/hello.cp) or [methods.cp](./examples/methods.cp)!

## Code generation examples

General struct showcase's code generation:
```c
/* Generated by C+ compiler. DO NOT EDIT. */

#ifndef CMPL__PTR_SIZE
#if defined(_WIN64) || defined(__x86_64__)
#define CMPL__PTR_SIZE 8
#else
#define CMPL__PTR_SIZE 4
#endif
#endif

#ifndef true
#define true 1
#endif
#ifndef false
#define false 0
#endif


    typedef struct app_Player
    {
        int health;
    } app_Player;


        void app_Player_damage(app_Player *self, int amount) // no need to name the first parameter self, can also be this or whatever! But it has to be there
        {
            self->health -= amount;
        }


int main()
{
    app_Player player = (app_Player){.health = 100,
    };
    app_Player_damage(&player,10);
    return 0;
}
```

"Hello, World!" showcase's code generation:
```c
/* Generated by C+ compiler. DO NOT EDIT. */

#ifndef CMPL__PTR_SIZE
#if defined(_WIN64) || defined(__x86_64__)
#define CMPL__PTR_SIZE 8
#else
#define CMPL__PTR_SIZE 4
#endif
#endif

#ifndef true
#define true 1
#endif
#ifndef false
#define false 0
#endif

// libc+.hp

#ifndef _LIBCP_HP
#define _LIBCP_HP

#ifdef _WIN32
#include <windows.h>
#elif defined(__APPLE__)
#include <TargetConditionals.h>
#else
#include <unistd.h>
#endif // #ifdef _WIN32

// io.hp

#ifndef _IO_HP
#define _IO_HP

// types.hp

#ifndef _TYPES_HP
#define _TYPES_HP


    /*
        Integer types
    */
    typedef unsigned char       std_uint8_t;
    typedef unsigned short      std_uint16_t;
    typedef unsigned int        std_uint32_t;
    typedef unsigned long long  std_uint64_t;

    typedef signed char         std_int8_t;
    typedef signed short        std_int16_t;
    typedef signed int          std_int32_t;
    typedef signed long long    std_int64_t;

    /*
        Pointer-sized types
    */
#if CMPL__PTR_SIZE == 8
    typedef std_uint64_t std_ptrsize_t;
    typedef std_int64_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 4
    typedef std_uint32_t std_ptrsize_t;
    typedef std_int32_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 2
    typedef std_uint16_t std_ptrsize_t;
    typedef std_int16_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 1
    typedef std_uint8_t std_ptrsize_t;
    typedef std_int8_t  std_sptrsize_t;

#else
    char UNSUPPORTED_PTR = '\1';

#endif
    typedef std_ptrsize_t std_size_t;
 // namespace std

#endif // #ifndef _TYPES_HP

// __utilities.hp


#ifndef ___UTILITIES_HP
#define ___UTILITIES_HP

// types.hp

#ifndef _TYPES_HP
#define _TYPES_HP


    /*
        Integer types
    */
    typedef unsigned char       std_uint8_t;
    typedef unsigned short      std_uint16_t;
    typedef unsigned int        std_uint32_t;
    typedef unsigned long long  std_uint64_t;

    typedef signed char         std_int8_t;
    typedef signed short        std_int16_t;
    typedef signed int          std_int32_t;
    typedef signed long long    std_int64_t;

    /*
        Pointer-sized types
    */
#if CMPL__PTR_SIZE == 8
    typedef std_uint64_t std_ptrsize_t;
    typedef std_int64_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 4
    typedef std_uint32_t std_ptrsize_t;
    typedef std_int32_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 2
    typedef std_uint16_t std_ptrsize_t;
    typedef std_int16_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 1
    typedef std_uint8_t std_ptrsize_t;
    typedef std_int8_t  std_sptrsize_t;

#else
    char UNSUPPORTED_PTR = '\1';

#endif
    typedef std_ptrsize_t std_size_t;
 // namespace std

#endif // #ifndef _TYPES_HP



/*

    HELPERS

*/

    std_size_t std__chpLength(const char *const _chpString)
    {
        std_size_t total = 0;
        for (;_chpString[total];total++){}
        return total;
    } // std::size_t std::_chpLength(const char *const _chpString)

    std_size_t std__chpLengthPadding(const char *const _chpString)
    {
        std_size_t total = 0;
        for (;_chpString[total];total++){}
        return total+1;
    } // std::size_t std::_chpLengthPadding(const char *const _chpString)
 // namespace std

#endif // #ifndef ___UTILITIES_HP


#ifdef _WIN32
#include <windows.h>
#else
#include <unistd.h>
#endif



    void std_write_stdout(const char *const _chpString, std_ptrsize_t _pszLength)
    {
#ifdef _WIN32
        DWORD written = 0;

        WriteFile(
            GetStdHandle(STD_OUTPUT_HANDLE),
            _chpString,
            (DWORD)(_pszLength),
            &written,
            ((void *)0)
        );
#else
        write(STDOUT_FILENO, _chpString, _pszLength);
#endif
    } // void std::write_stdout(const char *const _chpString, std::ptrsize_t _pszLength)

    void std_write_stderr(const char *const _chpString, std_ptrsize_t _pszLength)
    {
#ifdef _WIN32
        DWORD written = 0;

        WriteFile(
            GetStdHandle(STD_ERROR_HANDLE),
            _chpString,
            (DWORD)(_pszLength),
            &written,
            ((void *)0)
        );
#else
        write(STDERR_FILENO, _chpString, _pszLength);
#endif
    }

    /**
     * Puts a string to stdout
     **/
    void std_puts(
        const char *const _chpString
    )
    {
        std_write_stdout(_chpString, std__chpLength(_chpString));
    } // void std::puts(const char *const _chpString)

    /**
     * Puts a character to stdout 
     **/
    void std_putc(
        const char _chChar
    )
    {
        std_write_stdout(&_chChar, 1);
    } // void std::putc(const char _chChar)

    /**
     * Puts a string to stderr
     **/
    void std_errputs(
        const char *const _chpString
    )
    {
        std_write_stderr(_chpString, std__chpLength(_chpString));
    } // void std::errputs(const char *const _chpString)

    /**
     * Puts a character to stderr
     **/
    void std_errputc(
        const char _chChar
    )
    {
        std_write_stderr(&_chChar, 1);
    } // void std::errputs(const char _chpChar)

 // namespace std

#endif // #ifndef _IO_HP

// __utilities.hp


#ifndef ___UTILITIES_HP
#define ___UTILITIES_HP

// types.hp

#ifndef _TYPES_HP
#define _TYPES_HP


    /*
        Integer types
    */
    typedef unsigned char       std_uint8_t;
    typedef unsigned short      std_uint16_t;
    typedef unsigned int        std_uint32_t;
    typedef unsigned long long  std_uint64_t;

    typedef signed char         std_int8_t;
    typedef signed short        std_int16_t;
    typedef signed int          std_int32_t;
    typedef signed long long    std_int64_t;

    /*
        Pointer-sized types
    */
#if CMPL__PTR_SIZE == 8
    typedef std_uint64_t std_ptrsize_t;
    typedef std_int64_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 4
    typedef std_uint32_t std_ptrsize_t;
    typedef std_int32_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 2
    typedef std_uint16_t std_ptrsize_t;
    typedef std_int16_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 1
    typedef std_uint8_t std_ptrsize_t;
    typedef std_int8_t  std_sptrsize_t;

#else
    char UNSUPPORTED_PTR = '\1';

#endif
    typedef std_ptrsize_t std_size_t;
 // namespace std

#endif // #ifndef _TYPES_HP



/*

    HELPERS

*/

    std_size_t std__chpLength(const char *const _chpString)
    {
        std_size_t total = 0;
        for (;_chpString[total];total++){}
        return total;
    } // std::size_t std::_chpLength(const char *const _chpString)

    std_size_t std__chpLengthPadding(const char *const _chpString)
    {
        std_size_t total = 0;
        for (;_chpString[total];total++){}
        return total+1;
    } // std::size_t std::_chpLengthPadding(const char *const _chpString)
 // namespace std

#endif // #ifndef ___UTILITIES_HP

// types.hp

#ifndef _TYPES_HP
#define _TYPES_HP


    /*
        Integer types
    */
    typedef unsigned char       std_uint8_t;
    typedef unsigned short      std_uint16_t;
    typedef unsigned int        std_uint32_t;
    typedef unsigned long long  std_uint64_t;

    typedef signed char         std_int8_t;
    typedef signed short        std_int16_t;
    typedef signed int          std_int32_t;
    typedef signed long long    std_int64_t;

    /*
        Pointer-sized types
    */
#if CMPL__PTR_SIZE == 8
    typedef std_uint64_t std_ptrsize_t;
    typedef std_int64_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 4
    typedef std_uint32_t std_ptrsize_t;
    typedef std_int32_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 2
    typedef std_uint16_t std_ptrsize_t;
    typedef std_int16_t  std_sptrsize_t;

#elif CMPL__PTR_SIZE == 1
    typedef std_uint8_t std_ptrsize_t;
    typedef std_int8_t  std_sptrsize_t;

#else
    char UNSUPPORTED_PTR = '\1';

#endif
    typedef std_ptrsize_t std_size_t;
 // namespace std

#endif // #ifndef _TYPES_HP




    typedef enum std_OS
    {
        std_OS_Windows,
        std_OS_Linux,
        std_OS_MacOS,
        std_OS_FreeBSD,
        std_OS_OpenBSD,
        std_OS_NetBSD,
        std_OS_Android,
        std_OS_IOS,
        std_OS_Unknown
    } std_OS; // enum OS

    typedef enum std_Arch
    {
        std_Arch_X86,
        std_Arch_X86_64,
        std_Arch_AArch64,
        std_Arch_ARM,
        std_Arch_RISCV32,
        std_Arch_RISCV64,
        std_Arch_LoongArch64,
        std_Arch_PPC64,
        std_Arch_S390X,
        std_Arch_Wasm32,
        std_Arch_Wasm64,
        std_Arch_Unknown
    } std_Arch; // enum Arch
    
    typedef enum std_Endian
    {
        std_Endian_Little,
        std_Endian_Big
    } std_Endian; // enum Endian

    typedef struct std_Platform
    {
        std_OS __os;
        std_Arch __arch;
        std_Endian __endian;
        std_size_t __ptr_size;
    } std_Platform;


        std_OS std_Platform_os(std_Platform *self)
        {
            return self->__os;
        }


        std_Arch std_Platform_arch(std_Platform *self)
        {
            return self->__arch;
        }


        std_Endian std_Platform_endian(std_Platform *self)
        {
            return self->__endian;
        }


        std_size_t std_Platform_ptr_size(std_Platform *self)
        {
            return self->__ptr_size;
        } // struct Platform



    void std_hello()
    {
        std_write_stdout("Hello World!\n", 13 /* because lengthWithNull - 1 */);
    } // void std::hello()

    std_Platform std_platform()
    {
        std_Platform p;

        /* ---------------- OS ---------------- */

    #if defined(_WIN32) || defined(_WIN64)
        p.__os = std_OS_Windows;

    #elif defined(__ANDROID__)
        p.__os = std_OS_Android;

    #elif defined(__APPLE__)
        #if defined(TARGET_OS_IPHONE) && TARGET_OS_IPHONE
            p.__os = std_OS_IOS;
        #else
            p.__os = std_OS_MacOS;
        #endif

    #elif defined(__linux__)
        p.__os = std_OS_Linux;

    #elif defined(__FreeBSD__)
        p.__os = std_OS_FreeBSD;

    #elif defined(__OpenBSD__)
        p.__os = std_OS_OpenBSD;

    #elif defined(__NetBSD__)
        p.__os = std_OS_NetBSD;

    #else
        p.__os = std_OS_Unknown;
    #endif


        /* -------------- Architecture -------------- */

    #if defined(__x86_64__) || defined(_M_X64)
        p.__arch = std_Arch_X86_64;

    #elif defined(__i386__) || defined(_M_IX86)
        p.__arch = std_Arch_X86;

    #elif defined(__aarch64__) || defined(_M_ARM64)
        p.__arch = std_Arch_AArch64;

    #elif defined(__arm__) || defined(_M_ARM)
        p.__arch = std_Arch_ARM;

    #elif defined(__riscv) && (__riscv_xlen == 64)
        p.__arch = std_Arch_RISCV64;

    #elif defined(__riscv) && (__riscv_xlen == 32)
        p.__arch = std_Arch_RISCV32;

    #elif defined(__loongarch64)
        p.__arch = std_Arch_LoongArch64;

    #elif defined(__powerpc64__)
        p.__arch = std_Arch_PPC64;

    #elif defined(__s390x__)
        p.__arch = std_Arch_S390X;

    #elif defined(__wasm64__)
        p.__arch = std_Arch_Wasm64;

    #elif defined(__wasm32__)
        p.__arch = std_Arch_Wasm32;

    #else
        p.__arch = std_Arch_Unknown;
    #endif


        /* ---------------- Endianness ---------------- */

    #if defined(__BYTE_ORDER__) && defined(__ORDER_LITTLE_ENDIAN__) && defined(__ORDER_BIG_ENDIAN__)

        #if __BYTE_ORDER__ == __ORDER_LITTLE_ENDIAN__
            p.__endian = std_Endian_Little;
        #else
            p.__endian = std_Endian_Big;
        #endif

    #elif defined(_WIN32)

        /* Windows only targets little-endian architectures. */
        p.__endian = std_Endian_Little;

    #else

        /* Fallback: runtime detection */
        {
            unsigned int value = 1;

            if (*(unsigned char *)(&value))
                p.__endian = std_Endian_Little;
            else
                p.__endian = std_Endian_Big;
        }

    #endif

        p.__ptr_size = sizeof(void *);

        return p;
    } // std::Platform std::platform()
 // namespace std

#endif // #ifndef _LIBCP_HP


int main()
{
    std_hello();
    return 0;
}
```

## Any questions?
Maybe your question can be answered by the official C+ specification? Give it a shot and dive into [spec-c+](./spec-c+.md).